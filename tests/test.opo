<name: str> -> int?: getAge[
    name == "Vinícius" ? [
        some(20)
    ] : [
        none
    ]
]

<> -> void: main[
    getAge("Vinícius") => age: int?

    age ? [
        "Vinícius' age:  "+str(age.some) !!
    ] : [
        "Vinícius' age: unkown" !!
    ]
]
