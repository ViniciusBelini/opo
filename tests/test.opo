<name: str> -> int?: getAge[
    name == "Vinícius" ? [
        some(20)
    ] : [
        none
    ]
]

<> -> void: main[
    getAge("Vinícius") => age: int?

    age ? [
        age.some !!
    ]

    # error
    # age.some
]
